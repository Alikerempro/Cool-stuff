import pip._vendor.requests
import json

def get_weather(city, api_key):
    base_url = "http://api.openweathermap.org/data/2.5/forecast"
    params = {
        "q": f"{city},{state}",
        "appid": api_key,
        "units": "metric",
        "cnt": 5
    }

    response = pip._vendor.requests.get(base_url, params=params)
    data = response.json()

    # Check if the API request was successful
    if response.status_code == 200:
        # Extract and display the weather information
        for forecast in data['list']:
            date_time = forecast['dt_txt']
            temperature = forecast['main']['temp']
            weather_description = forecast['weather'][0]['description']

            print(f"{date_time}: {temperature}°C, {weather_description}")
    else:
        print(f"Error: {data['message']}")

api_key = '07f5ba036453b6a1cd3210ba276b2cea'

# Enter the city for which you want to fetch the weather report
state = input("Enter the state: ")
city = input("Enter the city: ")

get_weather(city, api_key)
